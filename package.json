{
  "name": "bimini-run",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && rsync -av --delete dist/ steveelliott@192.168.71.250:~/bimini-run-dist/ -e 'ssh -i ~/.ssh/macbook_pro' && ssh -i ~/.ssh/macbook_pro steveelliott@192.168.71.250 'source ~/.nvm/nvm.sh && pm2 restart bimini-run || pm2 serve ~/bimini-run-dist 5175 --name bimini-run --spa'"
  },
  "dependencies": {
    "phaser": "^3.87.0"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
